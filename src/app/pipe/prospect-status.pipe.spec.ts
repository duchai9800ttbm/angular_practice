import { ProspectStatusPipe } from './prospect-status.pipe';

describe('ProspectStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ProspectStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
